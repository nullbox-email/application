mode: ContinuousDeployment

next-version: 1.0.0

strategies:
  - ConfiguredNextVersion
  - Fallback

branches:
  main:
    regex: ^main$
    increment: Patch
    label: ""

  feature:
    regex: ^features?[/-]
    increment: Minor

  hotfix:
    regex: ^hotfix[/-]
    increment: Patch
