mode: ContinuousDeployment

next-version: 1.0.0

branches:
  main:
    regex: ^main$
    increment: Patch
    is-mainline: true

  feature:
    regex: ^feature[/-]
    increment: Minor

  hotfix:
    regex: ^hotfix[/-]
    increment: Patch
