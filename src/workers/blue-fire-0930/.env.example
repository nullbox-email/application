# Example env file for local development (copy to `.env`).
# Secrets should be stored via Wrangler when deployed:
# https://developers.cloudflare.com/workers/configuration/secrets/

# Must match the Fabric API config: `EmailIngress:HmacSecret`
EMAIL_HMAC_SECRET=

# Mailgun
MAILGUN_API_KEY=
